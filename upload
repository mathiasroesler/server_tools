#!/bin/bash

source server_functions

function print_usage() {
	printf "Usage: upload [-hr] [-p number] [-s number] file destination\n"
	printf "Copy file to remote server destination or multiple files to remote server destination.\n"
	printf "Options:\n"
	printf " -h, print help.\n"
	printf " -r, upload a folder.\n"
	printf " -p number, specify port number.\n"
	printf " -s number, select the desired server. \n"
	available_servers

return 0
}


RECURSIVE=false
PORT=false
port_number=22
server=NULL


# Parse options
while getopts ":rhp:s:" opt; do
	case $opt in
		r) 
			RECURSIVE='true' 
			;;
		h) 
			print_usage
			exit 
			;;
		P)
			port_number="${OPTARG}"
			PORT=true
		       	;;

		s)
			server_selection ${OPTARG}
			;;
		?)
			echo "upload: invalid option."
			echo "Try 'upload -h' for help."
			exit
			;;
	esac
done

# Check for a server.
if [[ $server == NULL ]]; then
	echo "upload: no server selected."
	echo "Try 'upload -h' for help."
	exit
fi

# Shift to first argument after options.
shift $(($OPTIND - 1))

if [[ $# -lt 2 ]]; then
	echo "upload: not enough arguments."
	echo "Try 'upload -h' for help."
	exit

fi

ARGS="$@"
destination=${@: -1}
files=${ARGS%${destination}}

if [[ $RECURSIVE == 'true' ]]; then
	print_info
	scp -rP $port_number $files $options $server$destination

else
	print_info
	scp -P $port_number $files $options $server$destination
fi

